/** 项目所有常量的key都应该包含在内 */
export const ConstEnums = {
  PROJECT_NAME: 'Wnl Admin',
} as const

/**
 * 布局相关常量
 */
export const LAYOUT_CONSTANTS = {
  /** header高度(px) */
  HEADER_HEIGHT: 50,
} as const
